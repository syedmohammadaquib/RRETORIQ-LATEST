# AI Mock Interview System Environment Variables

# ==========================================
# AI Analysis Configuration (Choose ONE)
# ==========================================

# Option 1: OpenAI (Higher cost, excellent quality)
VITE_OPENAI_API_KEY=your_openai_api_key_here

# Option 2: Google Gemini (99% cheaper, great quality) - RECOMMENDED
VITE_GEMINI_API_KEY=your_gemini_api_key_here
VITE_GEMINI_MODEL=gemini-1.5-flash
# Options: gemini-1.5-flash (faster, cheaper) or gemini-1.5-pro (better quality)

# Choose which service to use for analysis (gemini or openai)
VITE_AI_ANALYSIS_PROVIDER=gemini

# ==========================================
# Speech-to-Text Configuration
# ==========================================

# OpenAI Whisper (Primary - REQUIRED)
# Note: Uses VITE_OPENAI_API_KEY from above

# Google Cloud Speech-to-Text (Fallback - Optional)
VITE_GOOGLE_SPEECH_API_KEY=your_google_cloud_speech_api_key_here

# Firebase Configuration (Required for data storage)
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Speech Recognition Configuration (Optional)
VITE_PREFER_WHISPER=true
VITE_FALLBACK_TO_BROWSER=true
VITE_MAX_RECORDING_DURATION=180

# Session Configuration (Optional)
VITE_DEFAULT_QUESTIONS_PER_SESSION=5
VITE_ENABLE_SESSION_PERSISTENCE=true
VITE_AUTO_SAVE_INTERVAL=30000

# Development Configuration (Optional)
VITE_DEBUG_MODE=false
VITE_LOG_LEVEL=info

# Legacy API URL (for backward compatibility)
VITE_API_URL=http://localhost:5000/api

# If you deploy the backend to Vercel or another domain, point the frontend proxy to it:
# Example: VITE_API_PROXY_BASE=https://your-vercel-app.vercel.app/api
# Then rebuild the frontend before deploying hosting.